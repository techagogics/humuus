{
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "clean": "turbo clean && rm -rf ",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "nakama-backup-db": "pnpm --prefix ./apps/nakama/ run db-backup",
    "nakama-load-db": "pnpm --prefix ./apps/nakama/ run db-load-backup"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "prettier-plugin-tailwindcss": "^0.5.11",
    "turbo": "^2.0.9"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18"
  },
  "name": "with-tailwind"
}
